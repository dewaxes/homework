//1
#include <iostream>
using namespace std; 
int main() {
    setlocale(LC_ALL, "RU");
//    int n;
//    cout << "Enter an integer: ";
//    cin >> n;
//    int array[3];
//    array[0] = n;
//    array[1] = n * 2;
//    array[2] = array[1] * 2;
//
//    for (int i = 0; i < 3; ++i) {
//        cout << array[i] << " ";
//    }
//
//    return 0;
//}
//2
    int num;
    cout << "Введите число: ";
    cin >> num;
    int arr[3][2] = { {num, num + 1}, {num + 1, num + 2}, {num, num} };

    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 2; j++) {
            cout << arr[i][j] << " ";
        }
        cout << endl;
    }

    return 0;
}
